//DOM - Document Object Model.
//Document is an html page 
//The HTML DOM model is a tree of Objects
//DOM is used to create a dynamic HTML elements and attributes also CSS styles
//Add or remove HTML elements 
//Creats new events

//The document is an in-built key word
// const document = "Hello"; //Throws an error

const val = document;
console.log(val.all);
console.log(val.doctype);
console.log(val.domain);
console.log(val.URL);
console.log(val.body);
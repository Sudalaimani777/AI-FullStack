const userName = "Sudalaimani Kannan";
console.log(userName);

const userAge = 22;
console.log(userAge);

// const a = 10;
// const b = 20;
// console.log(a / b); //Returns type error , because the typescript expects the datatype of the variable when the code executes

// console.log(a * b);

// To slove these kinda issue, we explicitely tells the datatypes of the particular varialble holds, the syntax will be -> 

const firstNumber : number = 22;
const secondNumber : number = 73;
console.log(firstNumber / secondNumber);
console.log(firstNumber * secondNumber);